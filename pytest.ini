[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: unusually slow test (optional: pytest -m "not slow")

# Parallel runs (CI uses -n auto): pytest tests/ -n auto
# Single-process debugging: pytest tests/ -n0 --pdb
